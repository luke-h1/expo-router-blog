/**
 * Native stub for usePreloadImages hook
 * On native platforms, this is a no-op as preloading is handled differently
 */
export function usePreloadImages(_imageUrls: string[]) {
  // No-op on native - expo-image handles preloading automatically
}
